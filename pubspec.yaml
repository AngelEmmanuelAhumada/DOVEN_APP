name: preferenciasusuarioapp
description: A new Flutter project.


version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  

  
  cupertino_icons: ^0.1.2
  intl: ^0.15.8
  moor_flutter: ^1.6.0
  provider: ^3.0.0+1
  font_awesome_flutter: ^8.2.0
  charts_flutter: ^0.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  moor_generator: # use the latest versions
  build_runner:
  shared_preferences:
  http:

flutter:

  uses-material-design: true

  assets:
   - assets/menu-img.jpg
  